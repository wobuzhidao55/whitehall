<div class="format-advice">
  <p><strong>Use this format for:</strong> Press releases, news stories and responses to media coverage including official statements.</p>

  <p>Do <em>not</em> use for: promoting the publication of other content (eg statistics).</p>
</div>

<%= edition_editing_tabs(edition) do %>
  <%= standard_edition_form(edition) do |form| %>
    <%= render 'inline_attachments_info', form: form, edition: edition %>
  <% end %>
<% end %>
