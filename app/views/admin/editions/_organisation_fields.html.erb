<% required = false unless defined?(required) %>
<%= form.label :lead_organisation_ids, "Select the organisations that are associated with this #{edition.format_name.titleize.downcase}", required: required %>
<% cache_if edition.lead_organisation_ids.empty?, taggable_organisations_cache_digest do %>
    <%= form.select :lead_organisation_ids,
                    options_for_select(taggable_organisations_container, edition.lead_organisation_ids),
                    { include_blank: true },
                    multiple: true,
                    class: 'chzn-select form-control',
                    data: { placeholder: "Choose organisations that are associated with this document…"} %>
<% end %>
