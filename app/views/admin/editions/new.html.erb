<% page_title "New " + @edition.type.titleize %>

<div class="row">
  <section class="col-sm-8">
    <h1>Create <%= @edition.type.underscore.humanize.downcase %></h1>
    <%= render partial: "form", locals: {edition: @edition} %>
  </section>
  <div class="col-sm-4">
    <%= simple_formatting_sidebar hide_inline_attachments_help: !@edition.allows_inline_attachments?,
                                  show_attachments_tab_help: true %>
  </div>
</div>
