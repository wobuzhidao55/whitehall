<% page_title "New group" %>

<div class="row">
  <section class="col-sm-8">
    <h1>New governance group in <%= @group.organisation.name %></h1>
    <%= render partial: "form", locals: {group: @group} %>
  </section>
  <div class="col-sm-4">
    <%= simple_formatting_sidebar %>
  </div>
</div>
