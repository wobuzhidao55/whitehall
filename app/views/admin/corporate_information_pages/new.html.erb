<% page_title "New corporate information page for " + @organisation.name %>

<div class="row">
  <section class="col-sm-8">
    <h1>Add new corporate information page to <%= link_to @organisation.name, [:admin, @organisation] %></h1>
    <%= render partial: "form", locals: {edition: @edition} %>
  </section>

  <div class="col-sm-4">
    <%= simple_formatting_sidebar %>
  </div>
</div>
